---
description: Audit a spreadsheet for formula accuracy, errors, and common mistakes
argument-hint: "[path to spreadsheet or 'current model']"
---

Load the `audit-xls` skill and run a full audit on the specified spreadsheet or current model in context.

If a file path is provided, use it. Otherwise audit the model most recently discussed or built in this session.
